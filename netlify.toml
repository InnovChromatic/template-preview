[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA: send every path (including /demo and /preview iframe URLs) to index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
